spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        show_sql: true
    open-in-view: false
  
  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
    com.swyp10: debug
    org.springframework: debug
    root: info

server:
  port: 0

# JWT 설정 (테스트용)
jwt:
  secret: test-secret-key-for-jwt-token-must-be-at-least-256-bits-long-for-security-purposes
  expiration: 3600 # 1시간
  refresh-expiration: 7200 # 2시간

# OAuth 설정 (테스트용)
oauth:
  kakao:
    client-id: test-kakao-client-id
    redirect-uri: http://localhost:8080/test/kakao/callback
    token-url: https://kauth.kakao.com/oauth/token
    user-info-url: https://kapi.kakao.com/v2/user/me

# 환경변수 기본값 설정
KAKAO_CLIENT_ID: test-kakao-client-id
DB_USERNAME: sa
DB_PASSWORD: 
DB_NAME: testdb
JWT_SECRET: test-secret-key-for-jwt-token-must-be-at-least-256-bits-long-for-security-purposes
KAKAO_REDIRECT_URI: http://localhost:8080/test/kakao/callback
